{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "vspreview",
            "type": "shell",
            "command": "${command:python.interpreterPath}",
            "args": ["-m", "vspreview", "\"'${file}'\""]
        }
    ]
}
